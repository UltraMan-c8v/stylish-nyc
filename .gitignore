# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

public/img/src/
public/img/incoming/

.vercel

# Archived originals. ~100MB of 2K source PNGs and the untouched client
# photographs, kept outside public/ so they are never deployed. They are handed
# over as a separate folder rather than committed: git is the wrong place for
# a hundred megabytes of binaries that never change.
_source-images/
